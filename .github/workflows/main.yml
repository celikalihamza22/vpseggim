name: Mangoo VPS RDP → bore.pub:25854

on:
  workflow_dispatch:

jobs:
  tunnel:
    runs-on: ubuntu-latest
    steps:
      - name: Bore indir & kur
        run: |
          wget https://github.com/ekzhang/bore/releases/download/v0.6.0/bore-v0.6.0-x86_64-unknown-linux-musl.tar.gz
          tar xzf bore-v0.6.0-x86_64-unknown-linux-musl.tar.gz
          chmod +x bore

      - name: Tüneli ateşle (82.22.32.50:3389 → bore.pub:25854)
        run: |
          echo ""
          echo "══════════════════════════════════"
          echo "BAĞLANTI ADRESİ → bore.pub:25854"
          echo "IP                  → 82.22.32.50"
          echo "Kullanıcı          → Administrator (veya sen ne koyduysan)"
          echo "Şifre               → Mangoo panelindeki Windows şifren"
          echo "══════════════════════════════════"
          echo ""
          ./bore local 82.22.32.50:3389 --to bore.pub --port 25854
